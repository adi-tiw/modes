functions named as fn

variable//
let variable: int
| | instead of () => → closure arguments in Rust
unwrap() → "trust me this worked, give me the value"
:: → accessing something inside a module, like . in JS
